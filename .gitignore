node_modules/
.DS_Store
Thumbs.db
*.log
.vscode/

# original drop folder - these files now live in assets/ (kept on disk, not committed)
image&video/

# screen recordings dropped in for debugging - kept on disk, not committed
Updates/
